apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pure-api-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: px-fb-direct-access-nfsv4
  resources:
    requests:
      storage: 10Gi